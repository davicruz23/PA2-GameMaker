function DrawSetText(cor, fonte, halign, valign) {
    draw_set_color(cor);            // Define a cor
    draw_set_font(fonte);           // Define a fonte
    draw_set_halign(halign);        // Define o alinhamento horizontal
    draw_set_valign(valign);        // Define o alinhamento vertical
}